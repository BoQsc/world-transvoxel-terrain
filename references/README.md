# References

Downloaded papers, external repository checkouts, and large research artifacts
belong under:

```text
references/downloaded/
```

That directory is ignored by Git. Keep only small manifests, summaries, and
non-reconstructive notes in the repository.

Before substantial implementation, A1 must pin the required references and
inspect the old marching-cubes project for maintainability failures.
