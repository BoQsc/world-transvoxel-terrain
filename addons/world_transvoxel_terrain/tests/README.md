# Addon-Local Test Ownership

Addon-local smoke and contract tests belong here.

Final gameplay validation belongs in the later separate game repository.

A2 smoke uses `res://tests/a2_addon_smoke.gd` from the repository-level test
folder so it can run through Godot headless mode.
